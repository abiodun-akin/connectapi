// Jest setup file for MongoDB and other global test configuration
process.env.NODE_ENV = 'test';

// Add any global test setup here
beforeAll(async () => {
  // Initialize test database, mock services, etc.
});

afterAll(async () => {
  // Cleanup after tests
});
